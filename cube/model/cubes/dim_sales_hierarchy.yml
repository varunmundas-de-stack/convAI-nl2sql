cubes:
  - name: dim_sales_hierarchy
    sql_table: public.dim_sales_hierarchy
    data_source: default

    joins: []

    dimensions:
      - name: area_manager_name
        sql: area_manager_name
        type: string

      - name: sales_officer_name
        sql: sales_officer_name
        type: string

      - name: sales_rep_name
        sql: sales_rep_name
        type: string

      - name: zonal_manager_name
        sql: zonal_manager_name
        type: string

    measures:
      - name: count
        type: count

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

